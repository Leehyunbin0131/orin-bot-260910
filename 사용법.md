# 로봇 사용법 — 하드웨어 검사부터 음성 안내까지

기준: 2026-09-09, Orin의 ROS 2 Jazzy 환경.
명령은 Orin에 직접 연결한 터미널이나 Orin으로 SSH 접속한 터미널에서 입력합니다.
운영용 ROS 2 도메인은 **11**로 통일했습니다. `jazzy` 또는 워크스페이스 환경을 불러오면
`ROS_DOMAIN_ID=11`이 적용되며, 세 실행 스크립트와 하드웨어 검사 기본값도 11입니다.
이미 실행 중인 노드는 종료 후 다시 실행해야 합니다. 다른 PC에서 ROS 노드를 직접
실행할 때도 11로 맞추세요. 맥북의 웹 브라우저·SSH 포워딩에는 ROS 설치가 필요 없습니다.

```bash
cd ~/ros2_ws
```

자동 주행과 키보드 조종은 하나씩 실행합니다. 앞서 실행한 테스트와 베이스 제어가
종료된 것을 확인한 뒤 다음 명령을 실행합니다. 아래 `.sh` 파일은 ROS 환경을
자동으로 적용하며, 카메라를 사용하는 테스트는 RSUSB도 자동으로 적용합니다.

## 처음 사용하는 순서

| 순서 | 작업 | 완료 기준 | 상세 설명 |
| --- | --- | --- | --- |
| 1 | 하드웨어 검사 | 라이다·카메라·모터 검사 결과 확인 | 1절 |
| 2 | 실제 공간의 맵 작성 | 키보드로 이동하며 맵을 그리고 저장 성공 확인 | 8절 |
| 3 | 저장된 맵에 호실 등록 | 각 호실의 도착 위치·방향·이름 저장 | 9절 ① |
| 4 | 안내 모드 실행 및 초기 위치 지정 | 화면에 출발 가능 메시지 표시 | 9절 ② |
| 5 | 호실 버튼으로 주행 확인 후 음성 요청 | 지정한 호실에 도착하고 정지 | 9절 ③ |

맵과 호실은 저장되므로 매번 다시 만들 필요가 없습니다. 다음 사용부터는 아래 절차로 시작합니다.
사각형 주행과 거리·회전 서비스는 별도의 점검 도구이며, 안내를 시작할 때 함께 실행하지 않습니다.

## 매일 안내를 시작하는 방법

**1. Orin 터미널에서 안내 모드를 실행합니다.** 이전의 맵 작성·편집 모드·주행 프로그램은
종료되어 있어야 합니다. 전원을 켜는 것만으로 안내 프로그램이 자동 실행되지는 않습니다.

```bash
cd ~/ros2_ws
jazzy
echo "$ROS_DOMAIN_ID"   # 11인지 확인
ros2 launch orinbot_hardware guide.launch.py drive:=true
```

`jazzy` 명령을 찾지 못하는 새 환경에서는 다음 명령으로 환경을 불러옵니다.

```bash
source /opt/ros/jazzy/setup.bash
source ~/ros2_ws/install/setup.bash
export ROS_DOMAIN_ID=11
```

**2. 맥북의 별도 로컬 터미널에서 웹 화면 연결을 엽니다.** 주소는 평소 SSH에 사용하는
Orin 주소로 바꾸세요. 이미 같은 포워딩을 실행 중이면 다시 실행할 필요가 없습니다.

```bash
ssh -N -L 8080:127.0.0.1:8080 hyunlee@orin.local
```

맥북 브라우저에서 [안내 화면](http://127.0.0.1:8080)을 엽니다.
Orin 자체 브라우저를 쓰는 경우에는 SSH 포워딩 없이 같은 주소를 엽니다.

**3. GUI에서 위치를 지정하고 안내를 요청합니다.**

1. 마지막 선택 맵이 맞는지 확인합니다. 다른 맵이면 선택 후 **지도 불러오기**를 누릅니다.
2. **로봇 위치 찍기**로 실제 구동축 중앙의 위치를 찍고 전방 방향으로 드래그합니다.
3. **선택한 점을 로봇 현재 위치로 지정**을 누릅니다.
4. “음성 또는 호실의 안내 버튼으로 출발할 수 있습니다.”가 표시될 때까지 기다립니다.
5. 호실의 **안내** 버튼을 누르거나, **말하기 → “101호로 가줘” → 녹음 마치기**로 요청합니다.

**4. 종료할 때는 안내 중단 → 정지 확인 → Orin 안내 런치에서 `Ctrl+C` 순서로 진행합니다.**
그 뒤 브라우저와 맥북 포워딩 터미널을 닫습니다. 브라우저를 닫는 것만으로 주행이 취소되지는 않습니다.

### 터미널의 역할

| 화면·터미널 | 실행 위치 | 용도 |
| --- | --- | --- |
| 로봇 실행 터미널 | Orin 또는 Orin에 SSH 접속한 터미널 | 맵 작성·편집·안내 중 한 런치 실행 |
| 웹 연결 터미널 | 맥북 로컬 터미널 | SSH 포워딩을 켜 둠. 로봇 명령을 입력하는 창이 아님 |
| 키보드 조종 터미널 | 별도의 Orin 터미널 또는 SSH 세션 | 맵 작성 때만 추가로 사용. 안내 모드에서는 종료 |
| 웹 브라우저 | 맥북 또는 Orin | 맵 시작·저장, 호실 등록, 초기 위치 지정, 음성 요청·중단 |

## 명령어 목록

| 목적 | 명령 | 바퀴 동작 |
| --- | --- | --- |
| 하드웨어 상태 검사 | `./hardware_test.sh` | 회전하지 않음 |
| 모터 회전까지 검사 | `./hardware_test.sh --motion --wheels-lifted` | 짧게 회전, 바퀴를 띄워 고정한 상태에서 실행 |
| 엔코더 사각형 주행 | `./rectangle_test.sh` | 준비 후 자동 출발 |
| 센서 융합 상태 검사 | `./rectangle_fused_test.sh observe:=true` | 토크를 끈 상태로 검사 |
| 센서 융합 사각형 주행 | `./rectangle_fused_test.sh` | 준비 후 자동 출발 |
| 키보드 조종 | 아래의 두 터미널 명령 사용 | 키 입력으로 이동 |
| 호실 등록 GUI | `ros2 launch orinbot_hardware guide.launch.py` (9절) | 모터를 열지 않는 편집 모드 |
| 음성 안내 로봇 | `ros2 launch orinbot_hardware guide.launch.py drive:=true` (9절) | 호실·음성 요청 후 Nav2 주행 |
| 웹 GUI 맵 작성 | `ros2 launch orinbot_hardware mapping.launch.py` 후 브라우저 (8절) | 키보드로 수동 이동 |
| 거리·각도 지정 이동 | `ros2 launch orinbot_hardware motion_service.launch.py` 후 서비스 호출 (7절) | 요청한 거리·각도만 이동 |

사각형 주행·키보드 조종·서비스 이동에는 장애물 자동 회피 기능이 없습니다.
실제 이동 명령은 로봇이 움직일 공간을 확보한 상태에서 실행합니다.

## 1. 하드웨어 테스트

라이다, 카메라 컬러·정렬 깊이·IMU 수신과 다이나믹셀 두 개의 통신·오류·정지 상태를 검사합니다.

```bash
./hardware_test.sh
```

기본 센서 검사 시간은 30초입니다. 60초로 늘리려면:

```bash
./hardware_test.sh --duration 60
```

바퀴를 띄워 고정한 뒤 좌륜·우륜·양륜 회전과 엔코더 응답도 검사하려면:

```bash
./hardware_test.sh --motion --wheels-lifted
```

`--duration`은 10~120초입니다. 기본 검사의 모터 PASS는 통신·정지 상태 확인이며,
회전 검사를 포함하려면 위 두 옵션을 함께 지정합니다.

## 2. 엔코더 사각형 주행

```bash
./rectangle_test.sh
```

**준비 후 실제로 출발합니다.** 기본값은 2m × 2m, 직진 최대 50rpm입니다.
각 모서리에서 왼쪽으로 90도씩 회전하며, 엔코더 기반 `/odom`으로 거리와 각도를 판단합니다.

1m × 1m, 직진 최대 15rpm으로 지정하려면:

```bash
./rectangle_test.sh width:=1.0 height:=1.0 rpm:=15
```

| 옵션 | 기본값 | 범위·단위 |
| --- | ---: | --- |
| `width` | 2.0 | 0.1~5m |
| `height` | 2.0 | 0.1~5m |
| `rpm` | 50 | 바퀴 회전 속도 상한, 1~50rpm |

## 3. 센서 융합 사각형 주행

엔코더, 라이다 스캔 매칭 이동 추정, 카메라 IMU 각속도를 EKF로 합칩니다.
주행 판단에는 `/odometry/filtered`를 사용합니다.

토크를 끈 채 센서 융합만 확인하려면:

```bash
./rectangle_fused_test.sh observe:=true
```

관측 시간을 60초로 지정하려면:

```bash
./rectangle_fused_test.sh observe:=true duration:=60
```

**실제 사각형 주행**을 시작하려면:

```bash
./rectangle_fused_test.sh
```

기본값은 1m × 1m, 직진 최대 60rpm, 회전 시 바퀴 최대 25rpm입니다.
작은 경로에서 저속으로 비교하려면:

```bash
./rectangle_fused_test.sh width:=0.5 height:=0.5 rpm:=15 turn_rpm:=10
```

| 옵션 | 기본값 | 범위·의미 |
| --- | ---: | --- |
| `width`, `height` | 각각 1.0 | 각각 0.1~5m |
| `rpm` | 60 | 직진 바퀴 속도 상한, 1~60rpm |
| `turn_rpm` | 25 | 회전 바퀴 속도 상한, 1~25rpm. `rpm` 상한도 적용 |
| `observe` | false | true이면 주행 없이 센서 융합 검사 |
| `duration` | 30 | 관측 모드 검사 시간, 10~120초. 주행 시간 지정이 아님 |

관측 모드 PASS는 입력과 센서 융합의 동작 확인입니다. 실제 주행 거리·각도 정확도는
바닥에서 측정해야 합니다. 이 테스트는 지도를 작성하지 않습니다.

## 4. 키보드 조종

Orin에 연결된 터미널 두 개를 사용합니다. 기존 사각형 테스트와 베이스 제어는 종료합니다.

**터미널 1 — 베이스 제어**

```bash
cd ~/ros2_ws
jazzy
ros2 launch orinbot_hardware base.launch.py \
  enable_torque:=true rectangle_rpm:=15 rectangle_turn_rpm:=10
```

모터 토크를 켜고 속도 명령을 기다립니다. 여기의 `rectangle_rpm` 옵션은 베이스의
속도 제한을 설정하며, 이 명령 자체가 사각형 주행을 시작하지는 않습니다.

**터미널 2 — 키보드 입력**

```bash
cd ~/ros2_ws
jazzy
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args \
  -p stamped:=true \
  -p frame_id:=base_footprint \
  -p speed:=0.03 \
  -p turn:=0.10
```

영문 소문자 입력 상태에서 터미널 2에 포커스를 두고 조작합니다.
초기 전진 속도는 0.03m/s(초당 3cm), 회전 각속도는 0.10rad/s입니다.
`stamped:=true`는 현재 모터 제어기가 받는 `TwistStamped` 메시지 형식에 맞춘 설정입니다.

| 키 | 동작 |
| --- | --- |
| `i` | 전진 |
| `,` | 후진 |
| `j` / `l` | 제자리 좌회전 / 우회전 |
| `u` / `o` | 전진하며 좌회전 / 우회전 |
| `k` | 정지 명령 |
| `q` / `z` | 설정된 이동·회전 속도를 함께 10% 증가 / 감소 |
| `w` / `x` | 설정된 전진 속도만 10% 증가 / 감소 |
| `e` / `c` | 설정된 회전 속도만 10% 증가 / 감소 |
| `Ctrl+C` | 키보드 조종 종료 및 정지 명령 |

속도 증가 키를 눌러도 베이스 제어기의 속도 상한이 적용됩니다.
설치된 키보드 프로그램은 키 입력마다 명령을 발행하고, 베이스는 명령이 0.3초간
없으면 정지 처리합니다. 계속 이동하려면 방향 키를 누르고 있어야 하며, 키보드의
반복 입력 지연 때문에 처음에는 잠깐 움직였다 멈출 수 있습니다.

조종을 마칠 때는 `k`로 정지한 뒤 터미널 2에서 `Ctrl+C`, 바퀴 정지를 확인한 뒤
터미널 1에서도 `Ctrl+C`로 베이스 제어를 종료합니다.

`jazzy: command not found`가 나오면 해당 터미널에서 `jazzy` 대신 다음을 실행합니다.
두 터미널의 ROS 도메인은 같아야 합니다.

```bash
source /opt/ros/jazzy/setup.bash
source ~/ros2_ws/install/setup.bash
export ROS_DOMAIN_ID=11
```

## 5. 중지·도움말·로그

테스트를 중단하려면 실행한 터미널에서 `Ctrl+C`를 누르고, 정지 처리와 프로그램
종료가 끝날 때까지 기다립니다. 센서 융합 주행은 종료 시 정지 명령을 유지하며
엔코더로 정지를 확인하므로 종료에 수 초가 걸릴 수 있습니다.

각 실행 파일의 도움말:

```bash
./hardware_test.sh --help
./rectangle_test.sh --help
./rectangle_fused_test.sh --help
```

| 실행 | 결과 위치 (`~/ros2_ws/` 기준) |
| --- | --- |
| 하드웨어 검사 | `log/interface_tests/<실행 시각>/summary.json` |
| 엔코더 사각형 | `log/rectangle_runs/<실행 시각>/summary.json` |
| 센서 융합 사각형·관측 | `log/rectangle_fused_runs/<실행 시각>/summary.json` |
| 키보드 조종 | 베이스 실행 터미널 출력과 `~/.ros/log/`. 별도의 주행 결과 요약은 생성하지 않음 |

실행 시 터미널에 로그 경로가 표시됩니다. 실패하면 오류 문구와 해당 실행의
`summary.json`을 함께 확인합니다. 하드웨어 검사의 카메라 상세 출력은 같은 폴더의
`camera.log`에 있습니다.

키보드 프로그램 참고: [teleop_twist_keyboard 공식 사용법](https://github.com/ros2/teleop_twist_keyboard/blob/rolling/README.md).


## 6. 실물 로봇 모델 보기

맥북에서는 [회전 가능한 HTML 모델](log/robot_model/20260909/model-viewer.html)을
내려받아 브라우저로 열면 됩니다. 마우스 드래그로 회전하고 휠로 확대·축소할 수 있습니다.
[고정 미리보기 이미지](log/robot_model/20260909/model-preview.png)도 있습니다.

Orin의 데스크톱 터미널에서 RViz로 확인하려면:

```bash
cd ~/ros2_ws
jazzy
ros2 launch orinbot_hardware model.launch.py
```

모터 제어와 센서 드라이버를 실행하지 않는 모델 전용 명령입니다. 먼저 다른 테스트와
베이스 제어를 종료합니다. 중지는 실행 터미널의 `Ctrl+C`입니다. GUI가 없는 SSH에서는
RViz 창을 바로 볼 수 없으므로 위 HTML 파일을 사용하세요.

RViz 화면에서 왼쪽 드래그로 회전, 마우스 휠로 확대·축소할 수 있습니다.
주황색 구동륜은 폭을 임시로 3cm로 표시한 것이고, 캐스터 오프셋은 1cm로 근사했습니다.
캐스터 위 주황색 지지대는 높이 차이를 표시하는 개략 형상입니다. 내부 40cm 정육면체의 2020 프로파일 프레임이며 외부는 44cm입니다.
후방 캐스터 장착축은 후방 프로파일 가운데에서 좌우 각각 13cm(간격 26cm)입니다.
세부 홈·브래킷은 단순화했으며 Nav2용 충돌 외곽선은 다음 준비 단계에서 설정합니다.

## 7. 서비스 호출로 지정 거리·각도 이동

엔코더 오도메트리를 이용해 짧은 전진·후진·제자리 회전을 검증하는 기능입니다.
전진·후진은 **cm**, 회전은 **도(deg)**로 입력합니다. 회전 방향은 위에서 보았을 때
왼쪽(반시계)이 양수, 오른쪽(시계)이 음수입니다. 거리와 각도는 한 번에 하나만 지정합니다.
라이다·카메라 융합이나 장애물 회피는 사용하지 않습니다. 바퀴가 헛돌면 실제 이동량과
응답에 표시되는 엔코더 이동량이 다를 수 있습니다.

### 터미널 1 — 서비스와 베이스 실행

다른 주행 테스트·키보드 조종·모델 미리보기·베이스 제어를 먼저 종료한 뒤 실행합니다.

```bash
cd ~/ros2_ws
jazzy
ros2 launch orinbot_hardware motion_service.launch.py
```

모터 토크가 켜지고 명령을 기다리며, 이동 서비스 호출 전에는 출발하지 않습니다.
직진 속도는 최대 3cm/s, 회전 속도는 최대 약 6.9도/s이며 목표에 가까워지면 감속합니다.
각 바퀴 명령은 최대 10rpm 범위로 제한합니다. 별도로 `base.launch.py`를 실행할 필요는 없습니다.

실제 모터를 연결하지 않는 모의 실행은 대신 다음 명령을 사용합니다.

```bash
ros2 launch orinbot_hardware motion_service.launch.py mock_hardware:=true
```

### 터미널 2 — 명령 호출

```bash
cd ~/ros2_ws
jazzy
```

먼저 준비 상태를 확인합니다. 응답 JSON에서 `ready: true`이면 명령을 받을 수 있습니다.
준비 전에는 `readiness_issue`에 이유가 표시됩니다.

```bash
ros2 service call /robot/status std_srvs/srv/Trigger '{}'
```

전진 10cm:

```bash
ros2 service call /robot/move orinbot_hardware/srv/Move '{distance_cm: 10.0, angle_deg: 0.0}'
```

후진 10cm:

```bash
ros2 service call /robot/move orinbot_hardware/srv/Move '{distance_cm: -10.0, angle_deg: 0.0}'
```

왼쪽으로 제자리 회전 90도:

```bash
ros2 service call /robot/move orinbot_hardware/srv/Move '{distance_cm: 0.0, angle_deg: 90.0}'
```

오른쪽으로 제자리 회전 90도:

```bash
ros2 service call /robot/move orinbot_hardware/srv/Move '{distance_cm: 0.0, angle_deg: -90.0}'
```

숫자를 바꾸면 됩니다. 한 번의 요청 범위는 거리 절댓값 **1~200cm**, 각도 절댓값
**1~360도**입니다. 모든 명령은 호출 시점의 위치·방향을 기준으로 합니다.
제자리 회전의 중심은 본체 중심보다 14cm 앞에 있는 구동축 중앙입니다.

`accepted: true`는 **명령 접수**이며 주행 완료를 뜻하지 않습니다. 응답의 `command_id`로
명령을 구분합니다. 이동·정지 확인 중에 새 명령을 보내면 `accepted: false`로 거절하며
대기열에 저장하지 않습니다. 다음 명령은 아래 상태 조회에서 완료를 확인한 뒤 보내세요.

```bash
ros2 service call /robot/status std_srvs/srv/Trigger '{}'
```

| 상태(`state`) | 뜻 |
| --- | --- |
| `idle` | 아직 실행한 이동 명령 없음. `ready`도 확인 |
| `moving` | 목표까지 이동 중 |
| `stopping` | 0 속도 명령을 보내며 정지 확인 중 |
| `succeeded` | 목표 도달과 정지 확인 완료 |
| `cancelled` | 사용자 중단 후 정지 확인 완료 |
| `failed` | 실패. `message`에서 원인 확인 |

상태 응답의 `distance_cm`는 시작 방향으로 투영한 엔코더 이동 거리이며,
`angle_deg`는 시작 방향 대비 누적 회전각입니다. 완료 판정은 정지 후 거리 오차 0.5cm
또는 각도 오차 1도 이내입니다. 이는 실제 바닥 위 위치 정확도를 보장하는 값은 아닙니다.

### 중단과 종료

진행 중인 이동을 중단하려면:

```bash
ros2 service call /robot/stop std_srvs/srv/Trigger '{}'
```

중단 응답도 요청 접수입니다. `/robot/status`에서 `cancelled`가 되면 정지가 확인된 것입니다.
오도메트리 단절, 5초간 진행 없음, 다른 속도 명령 발행자 감지 등은 실패 처리하고 정지
명령을 보냅니다. 정지 피드백을 확인할 수 없으면 `failed`와 해당 이유를 표시합니다.

터미널 2의 서비스 호출을 `Ctrl+C`로 끊는 것만으로는 이미 접수된 주행이 취소되지 않습니다.
중단 서비스를 호출하거나 터미널 1의 런치를 `Ctrl+C`로 종료하세요. 런치 종료 시 정지
명령과 베이스 종료 처리가 수행됩니다. 오도메트리 값·시간 오류가 기록된 경우 원인을
해결하고 터미널 1의 런치를 재시작합니다.

로그: `~/ros2_ws/log/motion_service/<실행 시각>/events.jsonl`
접수, 정지 시작, 최종 상태와 엔코더 이동량을 기록합니다.

## 8. 웹 GUI로 SLAM 맵 만들기

**실물용 2D 라이다 SLAM**입니다. `slam_toolbox`가 맵을 계산하고, 직접 만든 웹 화면이
지도·로봇 위치와 시작·저장 버튼을 제공합니다. RViz는 실행하지 않아도 됩니다.
카메라·IMU는 이번 SLAM 입력에 포함하지 않으며, 엔코더 오도메트리와 라이다를 사용합니다.

### ① Orin에서 맵 작업 환경 실행

이전에 켜 둔 베이스·거리 이동 서비스·사각형 테스트·모델 미리보기는 먼저 종료합니다.

```bash
cd ~/ros2_ws
jazzy
ros2 launch orinbot_hardware mapping.launch.py
```

베이스, 라이다, 맵 관리 서비스와 웹 화면을 실행합니다. 모터 토크는 켜지지만 자동으로
주행하지 않습니다. SLAM 계산은 화면의 **새 맵 작성 시작** 버튼을 누른 뒤 시작합니다.
이 런치가 베이스를 포함하므로 `base.launch.py`나 `motion_service.launch.py`를 별도로
실행하지 않습니다. 웹 화면 기본 주소는 `http://127.0.0.1:8080`입니다.

### ② 브라우저에서 GUI 열기

**Orin 데스크톱**에서는 브라우저로 [맵 작성 화면](http://127.0.0.1:8080)을 엽니다.

**맥북**에서는 새 로컬 터미널에서 아래 SSH 포워딩을 켜 두고, 맥북 브라우저에서 같은
주소를 엽니다. `orin.local`은 평소 SSH에 사용하는 Orin 주소 또는 IP로 바꾸면 됩니다.

```bash
ssh -N -L 8080:127.0.0.1:8080 hyunlee@orin.local
```

포워딩 터미널에 출력 없이 대기하는 것은 정상입니다. 기존 SSH 연결이나 Wi-Fi 설정을
변경하지 않습니다. 종료할 때 이 포워딩 터미널에서 `Ctrl+C`를 누릅니다.
웹 서버는 Orin 내부 주소에만 열려 있고, 포워딩을 통해 맥북에서 접근합니다.

화면에서 센서 준비가 끝나면 **새 맵 작성 시작** 버튼이 활성화됩니다. 로봇을 정지한
상태에서 누르고, 상태가 **맵 작성 중**으로 바뀌는지 확인합니다. 첫 지도 생성까지
수 초가 걸릴 수 있습니다. 실행 중 시작을 다시 요청해도 기존 맵을 초기화하지 않습니다.

지도에서는 드래그로 위치 이동, 휠·＋·－로 확대/축소, **전체 보기**로 화면 맞춤을 합니다.
주황색 화살표는 로봇의 위치와 전방 방향입니다. 흰색은 빈 공간, 검은색은 장애물,
회색은 아직 확인하지 못한 영역입니다. 지도·상태는 1초 간격으로 갱신합니다.

### ③ 다른 Orin 터미널에서 키보드 조종

```bash
cd ~/ros2_ws
jazzy
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args \
  -p stamped:=true \
  -p frame_id:=base_footprint \
  -p speed:=0.03 \
  -p turn:=0.10
```

키보드 조종 터미널에 포커스를 두고 `i` 전진, `,` 후진, `j`·`l` 좌우 회전,
`k` 정지를 사용합니다. 상세 키는 4절과 같습니다. 주행 키는 웹 화면에서 입력하지 않습니다.
방향 키를 누르고 천천히 이동하며 지도에 벽이 이어지는지 확인하세요. 처음에는 작은
구역을 돌아 시작 지점으로 되돌아오는 방식으로 확인합니다. 이 구성에는 장애물 자동
회피가 없습니다. 바퀴가 헛도는 곳에서는 지도가 왜곡되는지 확인하고 속도를 낮추세요.

### ④ 정지한 뒤 맵 저장

키보드에서 `k`를 눌러 정지한 뒤 GUI에서 **현재 맵 저장**을 누릅니다.
저장 성공 메시지와 폴더 경로를 확인합니다. 저장 후에도 맵 작성은 계속됩니다.
매번 새 폴더를 만들므로 이전 저장본을 덮어쓰지 않습니다.

```text
~/ros2_ws/maps/<저장 시각>/
  map.yaml         지도 해상도·원점·이미지 경로
  map.pgm          2D 지도 이미지 (설치된 저장기가 생성하는 이미지 형식)
  map.posegraph    SLAM 그래프
  map.data         SLAM 스캔 데이터
  metadata.json    저장 시각과 로봇·SLAM 설정
```

지도 이미지·YAML은 9절의 안내 GUI에서 불러와 주행에 사용합니다. 그래프·데이터는 추후
이어 그리기 기능에 사용할 수 있습니다. **맵 작성 GUI에서 저장된 SLAM 작업을 불러와
이어 그리는 기능은 아직 포함하지 않습니다.** 저장 도중 실패한 폴더에는 `.incomplete`가
남고 성공으로 표시하지 않습니다.

### 서비스 호출도 그대로 사용 가능

GUI 버튼은 다음 서비스와 같은 기능을 실행합니다. Orin에서 `jazzy` 후 호출합니다.

```bash
# 새 맵 작성 시작: 첫 지도 생성 확인 후 응답
ros2 service call /mapping/start std_srvs/srv/Trigger '{}'

# 현재 맵과 SLAM 원본 저장: 파일 생성 확인 후 경로 응답
ros2 service call /mapping/save std_srvs/srv/Trigger '{}'

# 실행 상태·센서 준비·마지막 저장 위치 확인
ros2 service call /mapping/status std_srvs/srv/Trigger '{}'
```

`/mapping/start`·`/mapping/save`의 `success: true`는 해당 작업이 완료됐다는 뜻입니다.
동시에 다른 시작·저장 작업을 요청하면 거절합니다. 지도 저장은 로봇이 정지하고 라이다·
오도메트리 입력이 정상일 때 실행합니다. 맵을 새로 그리려면 먼저 저장하고 런치를 종료한
뒤 다시 실행하세요.

### 종료·로그·선택 사항

`k`로 정지 → **현재 맵 저장** 성공 확인 → 키보드 프로그램 `Ctrl+C` → 맵 런치 `Ctrl+C`
순서로 종료합니다. 브라우저를 닫는 것만으로 맵 작성이나 로봇 프로그램이 종료되지는 않습니다.
저장하지 않은 진행 내용은 런치 종료 시 사라집니다. 런치 종료 시 베이스와 라이다, 해당
런치가 시작한 SLAM 프로세스를 함께 종료합니다.

SLAM 로그: `~/ros2_ws/log/mapping/<시작 시각>/slam.log`

8080 포트가 이미 사용 중이면 `gui_port:=8081`을 붙여 실행하고, 브라우저 주소와 SSH
포워딩의 두 포트 번호도 8081로 바꾸세요. `gui_port:=0`은 GUI를 끄고 서비스만 사용합니다.
저장 폴더는 `maps_directory:=/원하는/절대/경로`로 바꿀 수 있습니다.

RViz도 함께 보고 싶은 경우에만 Orin 데스크톱에서 `rviz:=true`를 붙여 실행합니다.
실물 SLAM의 TF는 `map → odom → base_footprint → 센서`이며, 기존 시뮬레이션용
`orinbot_navigation slam.launch.py`를 함께 실행하지 않습니다.

## 9. 호실 등록과 음성 안내 로봇

저장된 2D 맵에서 호실의 목적지를 등록하고, 노트북 마이크로 말한 호실까지 안내합니다.
음성은 **Orin 내부 Whisper.cpp 모델**로 변환하며 외부 음성 API를 호출하지 않습니다.
맵·호실 등록 GUI와 안내 GUI는 하나의 화면입니다. 맵 작성용 런치와 동시에 실행하지 않습니다.

### ① 저장 맵에 호실 등록 — 모터를 켜지 않는 편집 모드

```bash
cd ~/ros2_ws
jazzy
ros2 launch orinbot_hardware guide.launch.py
```

브라우저에서 `http://127.0.0.1:8080`을 엽니다. 맥북은 8절과 같은 SSH 포워딩을 사용합니다.

```bash
# 맥북의 로컬 터미널. orin.local은 평소 사용하는 Orin 주소로 바꿉니다.
ssh -N -L 8080:127.0.0.1:8080 hyunlee@orin.local
```

1. **저장된 지도** 목록에서 지도를 선택하고 **지도 불러오기**를 누릅니다.
2. **호실 찍기**를 선택하고 로봇이 도착해 설 수 있는 빈 공간을 클릭합니다.
   위치에서 원하는 전방 방향으로 드래그하면 도착 방향도 지정됩니다.
3. 호실 이름(예: `101호`), 필요하면 별칭(예: `상담실`)을 입력합니다.
4. **호실 저장**을 누릅니다. 지도에 초록색 목적지가 표시됩니다.
5. 다른 호실도 같은 방식으로 등록합니다. 목록의 **편집**으로 기존 위치·이름을 불러와
   수정하며, 같은 호실 이름으로 저장하면 그 호실의 좌표를 갱신합니다.

클릭만 하면 방향은 지도 좌표의 +X(0도)입니다. **도착 방향** 숫자로도 수정할 수 있습니다.
문·벽 위가 아니라 복도 쪽에 지정하세요. 현재는 회전 시 차체까지 고려해 목표 중심 주변
반경 46cm에 벽·미확인 영역이 있으면 등록을 거절하는 보수적인 기준을 사용합니다.
Nav2의 실제 경로 충돌 검사는 별도로 로봇의 비대칭 외곽선을 사용합니다.

맵 목록은 `~/ros2_ws/maps/` 아래의 `map.yaml`을 찾습니다. 목록이 비어 있으면 8절에서
실제 공간의 맵을 저장하세요. `log/...`의 검사 지도는 운영용 맵으로 자동 선택하지 않습니다.

호실은 해당 맵 폴더의 `rooms.json`에 저장됩니다. 맵 파일의 지문도 함께 저장해 다른 맵에
기존 좌표를 잘못 적용하지 않도록 합니다. 수정·삭제 전에는 `rooms.<시각>.bak.json` 백업을
만듭니다. 호실 이름·별칭이 다른 호실과 겹치면 등록하지 않습니다. 여러 층의 맵은 각각
등록할 수 있지만, 층간 이동·엘리베이터 호출은 구현하지 않았습니다.

### ② 로봇 안내 모드 실행

편집 모드를 `Ctrl+C`로 종료한 뒤 실행합니다. 다른 베이스·키보드·사각형·거리 서비스·SLAM도
종료되어 있어야 합니다.

```bash
jazzy
ros2 launch orinbot_hardware guide.launch.py drive:=true
```

이 명령은 실제 모터 베이스와 라이다를 켜고, 마지막에 선택한 맵을 자동으로 불러와
맵 서버·AMCL·Nav2를 실행합니다. **맵 로드 자체로 출발하지 않습니다.** 웹 GUI 주소는 같습니다.
현재 구성은 직진 최대 3cm/s, 회전 최대 약 6.9도/s이고, 각 바퀴 명령은 10rpm으로 제한합니다.

로봇을 켤 때마다 다음 초기 위치 지정이 필요합니다.

1. 로봇을 정지한 상태에서 **로봇 위치 찍기**를 선택합니다.
2. 지도에서 현재 **전방 구동축 중앙의 지면 투영점**에 해당하는 위치를 찍고,
   실제 전방 방향으로 드래그합니다. 본체 중심은 이 점보다 14cm 뒤에 있습니다.
3. **선택한 점을 로봇 현재 위치로 지정**을 누릅니다.
4. 화면의 주황 로봇 화살표가 실제 위치·방향과 맞는지 확인합니다.
5. Nav2 서버 활성화와 위치 추정이 확인되어 화면에 안내 가능 상태가 표시될 때까지 기다립니다.

종료 전 위치를 다음 실행 때 그대로 믿고 출발하지 않습니다. 로봇을 옮겼거나 전원이 꺼진
사이에 위치가 바뀔 수 있으므로 초기 위치를 다시 지정합니다. 잘못 지정하면 정지 상태에서
다시 지정하세요. 안내 중에는 위치·맵·호실을 변경할 수 없습니다.

### ③ 말하기 버튼으로 목적지 요청

1. 맥북 브라우저에서 **말하기**를 누르고, 처음이라면 마이크 사용을 허용합니다.
2. “101호로 가줘”, “백일호실로 안내해 줘”, “상담실로 가줘”처럼 말씀합니다.
3. **녹음 마치기**를 누릅니다. 최대 15초 후에는 자동으로 녹음을 마칩니다.
4. 인식한 문장과 목적지 호실이 화면에 표시되고, 등록된 목적지 하나를 찾으면 안내를 시작합니다.
5. 지도에서 로봇·계획 경로·안내 상태를 확인합니다. 도착 후 정지 피드백까지 확인하면 **도착**으로 표시합니다.

마이크는 노트북 브라우저의 마이크를 사용합니다. `localhost`로 접속해야 마이크 권한을
사용할 수 있으므로 맥북에서는 위 SSH 포워딩을 유지하세요. 브라우저 권한 설정에서
마이크가 차단되어 있다면 허용한 뒤 다시 누릅니다.

현재는 CPU에서 한국어 지원 `small-q5_1` 모델을 실행합니다. 짧은 말도 인식에 수 초 이상
걸릴 수 있습니다. 화면에 **인식 중**이 표시되는 동안 기다리세요. 같은 파일을 계속
녹음하지 않으며 녹음을 마치면 마이크 사용을 종료하고, 서버의 임시 음성 파일은 인식 후 삭제합니다.

등록되지 않은 호실, 여러 호실이 함께 들린 요청, 무음은 출발하지 않습니다. 호실 이름·별칭은
등록값과 대조하며 비슷한 이름을 임의로 추측하지 않습니다. `일공일호`·`백일호` 같은
대표적인 숫자 발음도 `101호`로 처리합니다. **인식 모델이 다른 등록 호실로 잘못 들을 가능성은
남아 있으므로 화면에 표시된 목적지를 확인하세요.** 잘못된 목적지라면 바로 **안내 중단**을 누릅니다.

등록된 호실의 **안내** 버튼이나 **문자로 요청하기**도 같은 Nav2 기능을 사용합니다.
먼저 버튼으로 짧은 목적지 주행을 검증한 뒤 음성 안내를 사용하면 문제를 구분하기 쉽습니다.
편집 모드에서는 호실 편집·음성 인식은 가능하지만 주행 요청은 거절합니다.

### 중단·오류·종료

**안내 중단**은 Nav2 목표를 취소하고 0 속도를 보내며 실제 엔코더 정지를 확인합니다.
“멈춰”, “정지”, “취소”도 인식되면 중단 요청으로 처리하지만, 음성 인식에는 시간이 걸리므로
즉시 멈춰야 할 때는 화면의 **안내 중단**을 사용하세요.
녹음·인식 중에 중단을 누르면 해당 음성 요청도 취소되며, 늦게 도착한 인식 결과로 출발하지 않습니다.

라이다·오도메트리 단절, 위치 추정 이상, Nav2 비활성화, 다른 속도 명령 발행자 감지 시
주행 명령을 차단하고 안내를 중단합니다. Nav2 오류 후에는 원인을 확인한 뒤 맵을 다시
불러오고 초기 위치를 지정해야 다시 출발할 수 있습니다. 장애물 때문에 경로를 찾지 못하면
실패하며, 자동 후진·회전 복구 동작을 반복하지 않습니다.

브라우저를 닫거나 SSH 포워딩만 끊어도 이미 접수된 안내는 계속됩니다. 종료하려면 먼저
**안내 중단** 후 정지를 확인하고, Orin에서 안내 런치를 `Ctrl+C`로 종료합니다.
이 안내 모드와 기존 키보드 조종을 동시에 사용하지 않습니다.

현재 장애물 입력은 높이 약 52cm의 2D 라이다입니다. 낮은 장애물·낙하 구간을 전부 감지하는
구성은 아니며, 실제 공간에서 센서 배치와 제동·직진·회전 오차를 확인해야 합니다.
카메라 깊이를 장애물 입력으로 추가하는 작업은 아직 포함하지 않았습니다.

### 파일과 설정

| 항목 | 위치 |
| --- | --- |
| 마지막 선택 맵 | `~/.local/share/orinbot/guide.json` |
| 호실·별칭·좌표 | `~/ros2_ws/maps/<맵 폴더>/rooms.json` |
| 안내 이벤트·Nav2 로그 | `~/ros2_ws/log/guide/<실행 시각>/` |
| 로컬 STT 실행 파일 | `~/.local/opt/whisper.cpp-v1.8.4/build/bin/whisper-cli` |
| 한국어 지원 모델 | `~/.local/share/orinbot/stt/ggml-small-q5_1.bin` |

STT는 현재 Orin에 설치했습니다. 다른 환경에 다시 설치할 경우 인터넷 연결 상태에서:

```bash
ros2 run orinbot_hardware setup_guide_stt.sh
```

설치 후 음성 인식에는 인터넷이 필요하지 않습니다. 빌드에는 `git`, `cmake`, C++ 컴파일러가 필요합니다.
`gui_port:=8081`, `maps_directory:=/원하는/맵폴더`, `settings_file:=/원하는/guide.json`으로
화면 포트·맵 폴더·선택 맵 설정 파일을 바꿀 수 있습니다.

소프트웨어 검사는 별도 합성 지도·가상 라이다·모의 모터에서 수행합니다. 실제 한국어
마이크 인식률과 건물 내 안내 주행 성능은 사용자 환경에서 확인해야 합니다.

## 10. 문제가 생겼을 때

| 증상 | 확인·조치 |
| --- | --- |
| `ros2` 또는 패키지를 찾지 못함 | Orin 터미널에서 `jazzy` 실행. 사용할 수 없으면 앞부분의 `source` 명령으로 환경을 불러옵니다. |
| 도메인이 11이 아님 | 새 터미널마다 `jazzy` 후 `echo "$ROS_DOMAIN_ID"` 확인. 이전 도메인으로 실행한 ROS 프로그램은 종료 후 다시 실행합니다. |
| 맥북에서 웹 화면이 열리지 않음 | Orin의 해당 런치와 맥북의 SSH 포워딩이 모두 실행 중인지, 브라우저가 `http://127.0.0.1:8080`인지 확인합니다. |
| 8080 포트 사용 중 오류 | 기존 맵·안내 런치나 포워딩의 중복 실행을 확인합니다. 다른 포트가 필요하면 런치에 `gui_port:=8081`을 붙이고 포워딩 양쪽 번호와 브라우저 주소도 8081로 맞춥니다. |
| 저장된 지도 목록이 비어 있음 | 8절에서 실제 맵을 저장하고 성공 경로를 확인합니다. 기본 검색 위치는 `~/ros2_ws/maps/`이며 저장 실패한 `.incomplete` 폴더는 제외됩니다. |
| 호실 저장이 거절됨 | 벽·미확인 영역에서 목표 중심을 46cm 이상 떨어뜨리고, 다른 호실과 이름·별칭이 겹치는지 확인합니다. |
| 안내 버튼이 비활성화되거나 요청이 거절됨 | `drive:=true`로 실행했는지 확인합니다. 맵을 선택하고 초기 위치를 지정한 뒤 화면의 준비 불가 사유를 확인합니다. |
| 위치를 지정해도 출발 가능 상태가 되지 않음 | 지도와 실제 장소·방향이 맞는지 확인하고 라이다·오도메트리·Nav2 상태가 준비될 때까지 기다립니다. 계속 실패하면 안내 로그를 확인합니다. |
| 말하기 버튼이 비활성화됨 | 맵을 먼저 불러오고 STT 준비 상태를 확인합니다. 설치가 없으면 9절의 STT 설치 명령을 사용합니다. |
| 마이크 사용 불가 | 맥북에서 SSH 포워딩을 통해 로컬 주소로 접속하고 브라우저의 마이크 권한을 허용합니다. |
| 인식은 됐지만 목적지를 찾지 못함 | 표시된 문장과 등록 호실·별칭을 대조합니다. 먼저 **문자로 요청하기** 또는 호실 **안내** 버튼으로 확인합니다. |
| 주행 중 오류로 멈춤 | **안내 중단** 후 정지를 확인하고 화면 메시지와 안내 로그를 확인합니다. 센서·Nav2 등의 원인을 해결한 뒤 맵을 다시 불러오고 초기 위치를 지정합니다. |

문의할 때는 실행한 명령, 화면 오류 문구, 해당 실행 시각의 로그 폴더를 함께 확인하면
원인을 구분하기 쉽습니다. 안내 로그는 `~/ros2_ws/log/guide/`, SLAM 로그는
`~/ros2_ws/log/mapping/`, 하드웨어 검사 결과는 `~/ros2_ws/log/interface_tests/`에 있습니다.
